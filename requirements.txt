PySide6>=6.6
# >=12.3: PIL's append-mode PDF writer (used by PDF flatten/enhance
# assembly) raises "trailer loop found" past ~4 pages on earlier versions.
Pillow>=12.3
pillow-heif>=0.18
pypdf>=4.3
pypdfium2>=4.30
xhtml2pdf>=0.2.16
openpyxl>=3.1
PyYAML>=6.0
